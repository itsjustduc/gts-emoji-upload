api_token = '' # the token for the gts/mastodon/... instance
instance = '' # url for the instance - should work both with and without trailing slash
working_dir = '' # the directory where the emojis are located - by default it is the current working directory (aka the folder in which u run the script in the terminal)
file_ext = ["png", "jpg", "jpeg", "gif"] # list of extensions the script should look for
